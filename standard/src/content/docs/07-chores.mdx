---
title: "§{chores} — Chores"
sidebar:
  order: 7
---
# 7. Chores [#chores]
> **Normative.** This chapter defines the `chore` top-level form: a thin specialization of `recipe` for command-runner-shaped bodies. The productions referenced here are given formally in App. A §{grammar-appendix.recipes} and §{grammar-appendix.steps}; where this chapter's prose disagrees with App. A, App. A is authoritative (§{notation.grammar-precedence}).

## 7.1. Chore header [#chores.header]
A chore is introduced by a **chore header** of the form `chore name (":" dependency_list)?`, where `name` is either a `BARE_IDENTIFIER` or a double-quoted `STRING` (App. A.5). An optional `:` introduces a dependency list (semantics identical to §{recipes.dep-list}).

The keyword `chore` MUST be followed by a separator — whitespace, a tab, or a `"` character — before the name. The keyword is recognised only at column 0 (§{lexical.keywords}); an indented `chore foo` line inside a recipe body or chore body is a `Content` token dispatched as a `shell_command`.

The reserved-segment rule of §{grammar-appendix.declarations} applies: a conforming implementation MUST reject a chore whose name's final dot-separated segment is one of `stem`, `name`, `ext`, `dir`, `in`, `out`. `all` is NOT among them (CS-0130): `chore all` is well-formed.

The **duplicate-name rule** of §{grammar-appendix.declarations} also applies: chores share the callable namespace with recipes, so a chore whose name matches that of an earlier `chore` or `recipe` declaration in the same Cookfile MUST be rejected at parse time. The diagnostic identifies both the offending declaration and the source line of the prior declaration.

A chore body is terminated by the implicit-termination rule of §{toplevel.termination}: it ranges from the line after the header to the next column-0 line classified as `recipe`, `chore`, `config`, `use`, or `import`, or end-of-file (whichever comes first).

### Example 7.1.1

```cook
chore clean
    rm -rf build

chore play: build
    fzf < build/menu.txt
```

Two chores: `clean` (no deps), `play` (depends on the recipe `build`).

## 7.1.1. Parameters [#chores.params]

A chore header MAY declare zero or more **parameters** between the chore name and the optional `:`. The full parameter grammar (including variadic and Lua-expression-default forms) is given in App. A.3.1; this subsection covers the **required** and **defaulted-string** forms.

A `param` is one of:

- **Required parameter:** a `BARE_IDENTIFIER`. Must be supplied at invocation as a positional argument.
- **Defaulted parameter (string literal):** `BARE_IDENTIFIER "=" STRING`. If no argv is supplied at this position at invocation time, the literal string binds.
- **Defaulted parameter (Lua expression):** `BARE_IDENTIFIER "=" "(" LUA_EXPR ")"`. If no argv is supplied at this position, the Lua expression is evaluated lazily against the Cookfile-scope Lua VM (§13.2 load phase). The result MUST be a string; a non-string return raises a diagnostic. The expression is evaluated only when argv is exhausted at this position — it is NOT evaluated when an explicit argv value is supplied. Note: Lua long-bracket strings (`[[...]]`) are NOT honoured inside the default expression in this version of the Standard; authors who need a long-bracket string inside a default MUST use a regular double-quoted string instead.

**Ordering rule (normative).** Within a `param_list`, every required parameter MUST precede every defaulted parameter (string-literal or Lua-expression form). A conforming implementation MUST reject a chore header that violates this order.

**Reserved-name rule (normative).** Parameter names MUST NOT equal one of the reserved identifiers from §A.2 (`stem`, `name`, `ext`, `dir`, `in`, `out`, `env`). Parameter names MUST NOT contain `.`. Duplicate parameter names within one header MUST be rejected at parse time. `all` is NOT reserved (CS-0130): a parameter named `all` is well-formed.

### Example 7.1.1.1

```cook
chore deploy target host="prod.example.com"
    > deploy.run(target, host)
```

`deploy` declares a required parameter `target` and a defaulted-string parameter `host`. Invoking `cook deploy production` binds `target="production"` and `host="prod.example.com"`.

**Introspection (informative).** A declared `param_list` is part of a chore's observable interface. A conforming implementation's human-readable workspace listing (e.g. `cook menu`) SHOULD surface each chore's declared parameters alongside its name — rendering a required parameter as its bare name, a defaulted-string parameter as `NAME="default"`, a required variadic as `NAME...`, and an optional variadic as `[NAME...]` — so the invocation shape is discoverable without opening the Cookfile. The machine-readable listing (`cook list`) remains bare names only. This is a presentation affordance, not a language-surface rule.

## 7.1.2. Parameter binding inside the body [#chores.param-binding]

When a chore body is executed, every declared parameter binds as a **Lua local** in the body's scope, visible to execute-Lua (`>`, `>{}`) and `lua_block` forms. The local's value is a Lua string for required and defaulted parameters; for variadics (§7.1.3) it is a Lua sequence (list-style table) of strings.

A conforming implementation MUST make these bindings available before any step of the chore body (including the first shell or `>` line) is executed.

Placeholder substitution (`$<NAME>`) is specified in §7.1.2.2. Environment-variable export to shell children is specified in §7.1.2.1 below.

**When parameters bind (normative).** At chore-invocation time, before the chore body's register phase runs:

1. The implementation collects positional argv after the chore name. Implementation-defined argv-partitioning rules — for example, sigil-marked config-preset selection or `--`-style end-of-options separators — MAY remove tokens from this stream before binding. This Standard does not prescribe a CLI surface for those rules; the reference Cook CLI implements them per the conventions described in its CHANGELOG and `cook --help` output, but a conforming implementation MAY choose other CLI surfaces.
2. argv is matched against the declared `param_list` in source order.
3. For each required parameter: argv at this position MUST exist; a missing element is a diagnostic per the table below.
4. For each defaulted parameter: if argv is exhausted at this position, the default is evaluated.
5. Excess argv elements MUST raise a diagnostic per the table below (unless a variadic absorbs them, §7.1.3).

| Condition | Diagnostic (recommended form) |
|---|---|
| Required arg missing | `chore 'NAME' requires parameter 'PARAM' (declared at line LINE); supply it as a positional argument` |
| Excess argv with no variadic | `chore 'NAME' takes K parameter(s) but M positional argument(s) were supplied` |
| Recipe receives argv | `recipe 'NAME': recipes do not take parameters; received N positional argument(s) (use '@PRESET' to select a config preset)` |
| Lua-expression default raises a Lua error | `chore 'NAME': default for parameter 'PARAM' raised a Lua error (defined at line LINE): MESSAGE` |
| Lua-expression default returns non-coercible value | `chore 'NAME': default for parameter 'PARAM' must evaluate to a string; got TYPE (defined at line LINE)` |

**Coercion of Lua-expression default return (normative).** Before the diagnostic above fires, a Lua-expression default's return value MUST be coerced to a string under the following rules: a Lua `string` is used verbatim; a Lua `number` (integer or float) is converted via Lua tostring semantics (e.g. `42` → `"42"`); a Lua `boolean` is converted to `"true"` or `"false"`. All other Lua types — including `nil`, `table`, `function`, `thread`, and `userdata` — MUST raise the non-coercible diagnostic.

### 7.1.2.1. Environment-variable export to shell children [#chores.param-env-export]

A conforming implementation MUST export every declared parameter as an environment variable into every shell child process spawned by the chore body. The export MUST occur for all parameter kinds (required, defaulted-string, defaulted-Lua, and variadic).

**Name mapping (normative).** The environment-variable name is the parameter name verbatim — no case normalisation or other transformation is applied.

**Value mapping (normative).**

- For **required** and **defaulted** parameters: the environment-variable value is the bound string value.
- For **variadic** parameters (§7.1.3): the environment-variable value is the space-joined string form of the bound list (no per-element quoting is applied, since environment variables are flat strings). For per-element access, use the Lua local (which is a sequence table) or the `$<NAME>` placeholder (which expands each element as a separately shell-quoted word).

**Empty variadic (`*NAME` with zero argv).** A `*NAME` variadic that binds the empty list MUST still be exported; the value is the empty string.

**Scope (normative).** The export applies to all shell steps in the chore body. The environment variable is visible to any sub-process spawned by the shell child (it is inherited through the normal POSIX environment inheritance mechanism).

### 7.1.2.2. Placeholder substitution in chore bodies [#chores.param-placeholders]

In a chore shell step, a `$<NAME>` placeholder resolves in this order:

1. If `NAME` is a parameter declared by the chore header, the placeholder expands to the bound value **quoted for the shell context in which the placeholder appears** (CS-0128): in an unquoted region it expands to a single POSIX-shell-quoted word; inside a double-quoted region it expands to the bound value escaped for double-quote context (backslash-escaping `\`, `"`, `$`, and the backtick) so the value sits literally inside the author's quotes (`echo "hello $<who>"` with `who="world"` yields `hello world`); inside a single-quoted region it expands verbatim (the author's `'...'` supplies the literal region — a value containing `'` is the author's responsibility). For a variadic parameter, each bound element is quoted the same context-aware way and the elements are joined by a single space.
2. Otherwise, the placeholder follows the ordinary closed-set placeholder resolution of §9 exactly as in a recipe body. Recipe references therefore resolve per §10.2 and establish the dependency edge required by §10.6; environment references resolve through the declared `env.NAME` namespace.

Parameters shadow recipe names and environment names. Resolution MUST NOT depend on whether the chore declares parameters: a `$<MODE>` env reference and a `$<build>` recipe reference behave the same in parameterless and parametric chores unless `MODE` or `build` is also a declared parameter.

## 7.1.3. Variadic parameters [#chores.variadic]

A chore MAY declare at most one **variadic parameter** at the tail of the `param_list`:

- `+NAME` — one-or-more variadic. At invocation, all remaining argv collect into a Lua sequence (list-style table); if zero remain, a diagnostic is raised.
- `*NAME` — zero-or-more variadic. At invocation, all remaining argv collect into a Lua sequence; if zero remain, the local binds to `{}`.

**Tail rule (normative).** A variadic parameter MUST be the final parameter in the `param_list`. A conforming implementation MUST reject a header with a non-variadic parameter after a variadic.

**Single-variadic rule (normative).** At most one variadic parameter is permitted per header.

**No-default rule (normative).** A variadic parameter MUST NOT carry an `=default`. The `*NAME` form is the way to express "optional variadic".

| Condition | Diagnostic (recommended form) |
|---|---|
| `+NAME` with zero argv | `chore 'NAME' requires one or more values for variadic '+PARAM' (declared at line LINE); supply at least one, or change to '*PARAM' to allow zero` |

## 7.2. Chore body — allowed and banned step kinds [#chores.body]

A chore body MAY contain any of the following step kinds:

| § | Step kind | Phase |
|---|---|---|
| §{steps.shell}     | `shell_command`       | execute (default-interactive, single-drain) |
| §{steps.lua}       | `lua_line`            | execute |
| §{steps.lua}       | `lua_block`           | execute |

A chore body MUST NOT contain any of the following step kinds:

| § | Step kind | Diagnostic guidance |
|---|---|---|
| §{steps.ingredients} | `ingredients_step` | The diagnostic identifies the keyword and directs the author to `recipe`. |
| §{steps.cook-single} | `cook_step` | The diagnostic identifies the keyword and directs the author to `recipe`. |
| §{steps.plate} | `plate_step` | The diagnostic identifies the keyword and directs the author to `recipe`. |
| §{steps.test} | `test_step` | The diagnostic identifies the keyword and directs the author to `recipe`. |

A conforming implementation MUST reject a chore body that contains any of the four banned step kinds. The diagnostic MUST identify the offending step's line and the offending keyword by name; the recommended message form names the keyword and suggests the `recipe` form.

**No register-phase steps (CS-0134).** CS-0134 removes the register-phase `inline_lua_line` (`>>`) and `inline_lua_block` (`>>{`) steps from the language entirely (§{lexical.line-prefixes}, Note 3.7.1). A chore body therefore admits **only** the four execute-phase step kinds above. Because there is no declarative-region step kind left in a chore body, the pre-CS-0134 region-ordering rule (§{recipes.region-rule}) is satisfied **vacuously** for chore bodies — there is no declarative region to place before the imperative steps. A conforming implementation MUST reject a `>>` or `>>{` line wherever it appears, including inside a chore body, with the migration diagnostic of §{lexical.line-classification}.

**Migration note (CS-0134).** Prior to CS-0072 a chore body admitted a `module_call` step (register-phase). CS-0072 removed it, and pre-CS-0134 authors reached for the `>>` / `>>{` inline-Lua prefixes to invoke a module function from a chore body. Both prefixes are now gone. An author who needs **register-phase** module-call orchestration MUST move it to a top-level `register_block` (§{decl.register}) or a top-level `module_call` (§{toplevel.module-call}); an author who wants to call a module function at **execute** time from a chore body writes it as an execute-Lua step — `> ns.fn(...)` or inside a `>{ … }` block — which runs on the chore's drain thread (§{chores.default-interactive}).

## 7.3. Default-interactive shell semantics [#chores.default-interactive]

Every `shell_command` step inside a chore body is **interactive**: it runs with the controlling terminal inherited, so the spawned child sees a real TTY (§{steps.shell}). This is the chore default; there is no marker to write. CS-0134 removed the `@` interactive prefix from the language entirely (§{lexical.line-prefixes}) — it was a redundant no-op, since every chore shell command is already interactive by this rule. A chore therefore inherits the TTY-allocation and stdio-inheritance semantics that the drain rule (§{exec.interactive-drain}) specifies for an interactive unit.

The execute-Lua step phases (`>` / `>{`) are unchanged by this rule. An execute-phase Lua step inside a chore body runs on the chore's drain thread (the controlling terminal), not on a worker VM — chore bodies share a single drain across all imperative-region steps including Lua bodies (CS-0051). The body-bundling rule (§{exec.body-bundling}) does NOT apply to chore bodies — as of CS-0134 it is vacuous even for recipes (§{exec.body-bundling}); chore-body execution is governed entirely by the single-drain rule of this section.

A chore body MUST execute as a **single drain** (CS-0051): once the first imperative-region step of the body begins, no work unit of any other recipe MAY run until the body completes — successfully or with a failure. The body's shell steps and execute-phase Lua steps run in source order on the controlling terminal under one shared drain window. The interactive-drain rule of §{exec.interactive-drain} governs each individual interactive unit; the chore-body case additionally shares a single drain across all of a chore's imperative steps (its shell and execute-Lua steps), rather than draining them one at a time.

### Example 7.3.1

```cook
chore deploy
    cd build
    rsync -av out/ user@host:/srv/app/
    > print("deploy complete")
```

The two shell steps are interactive: `cd build` and `rsync ...` run with the user's TTY inherited. The `>` line runs at execute phase on the drain thread (controlling terminal), not on a worker VM (CS-0051). There is no body-bundling, and the two shell steps and the `>` Lua line execute under one shared drain — no work unit of any other recipe interleaves with them (CS-0051).

## 7.4. No caching [#chores.no-caching]

A chore body MUST NOT register a cached work unit. This is satisfied trivially by the parse-time ban on `cook` and `plate` steps (§{chores.body}); a chore has no declarative cook step that would produce a cacheable output. As of CS-0134 a chore body admits no register-phase step at all (§{chores.body}), so it cannot record a work unit from its own body: the register-only helper `cook.add_unit` (§{lua.add-unit}) is unreachable from a chore body, and calling it from an execute-Lua step (`>` / `>{`) raises the execute-phase diagnostic of §{rationale.execute-phase-api} regardless of the `cache` flag.

This rule preserves the chore-vs-recipe semantic distinction: chores always run when invoked; they never short-circuit on a cache hit.

### Note 7.4.1

The engine does not need a chore-specific code path. A chore compiles to a recipe-body Lua function whose registered units are all `cache = false` and whose shell steps default to `interactive = true`. The distinction is a parse-time / codegen-time layer over the existing engine.

## 7.5. Cross-form dependencies [#chores.cross-form-deps]

A chore MAY depend on a recipe (run a build, then post-process) and a recipe MAY depend on a chore (rare but legal — for example, a recipe that depends on a one-off database-seed chore). Dependency resolution and ordering follow the rules of §{xref}; the form of the depender and depended-upon does not affect the resolution algorithm.

### Example 7.5.1

```cook
recipe build
    ingredients "src/*.c"
    cook "build/$<in.stem>.o" { cc -c $<in> -o $<out> }
    cook "build/app" { cc $<in> -o $<out> }

chore play: build
    ./build/app

recipe install: play
    cook "/usr/local/bin/app" { cp build/app $<out> }
```

`play` depends on the recipe `build`; the recipe `install` depends on the chore `play`. The graph: `build` (recipe, cached) → `play` (chore, never cached) → `install` (recipe, cached). Each edge is a normal name reference; no special wording for cross-form.

### 7.5.1. Dependencies on parametric chores [#chores.parametric-deps]

A recipe or chore MAY depend on a parametric chore (§7.1.1). The dependency edge supplies no argv, so the depended-upon chore runs as if invoked with no positional arguments: defaulted parameters bind their defaults (§7.1.1), zero-or-more variadics (`*NAME`) bind to the empty sequence, and any required parameter — or one-or-more variadic (`+NAME`) — without a satisfying argv is a configuration error rejected at register time with the same diagnostic class as the invocation-time errors of §7.1.2.

Supplying explicit argv from a dependency edge (e.g. `recipe X: (helper foo bar)`) is reserved for a future revision and is not part of this Standard.

#### Note 7.5.1.1 — Scope of register-time parameter checks (informative)

The register-time validation described above applies only to parametric chores **reachable from the dispatch target via the `requires` graph** (§{xref}) — i.e. the target itself plus its transitive dependencies. A parametric chore that declares a required parameter (or `+NAME` variadic) and is **not** in that reachable set is not validated at register time during the current dispatch: its body is not invoked and its parameters are not bound. This keeps unrelated chores in the same Cookfile from poisoning every other invocation; declaring `chore deploy target` does not block `cook greet alice` from running.

Implementations MAY still register, list, and diagnose syntactic problems with unreachable parametric chores (so that `cook list`, `cook menu`, and `cook dag` surface them); only the bind-argv-to-parameters step is scoped to the reachable set.
