probe scenes
    >{ return { { id = "a" }, { id = "b" } } }

recipe render
    gather scenes
    cook "build/$<in.id>.out" { echo "$<in.id>" > $<out> }

recipe mux
    gather scenes
    cook "build/$<in.id>.mp4" { cat $<render[]> > $<out> }
