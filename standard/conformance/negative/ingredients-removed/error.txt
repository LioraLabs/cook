`ingredients` was removed (CS-0229).
For determinants (the command never names these files):
    files core:src
        "src/**/*.rs" "Cargo.toml"
    recipe build
        seal core:src
For iteration (the command consumes each file):
    gather "tests/*.txt"
