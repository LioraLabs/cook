recipe "libmath"
    gather "src/math/*.c"
    cook "build/lib/$<in.stem>.o" { cc -c $<in> -o $<out> }

recipe "build"
    gather "src/*.c"
    cook "build/$<in.stem>.o" {
        gcc -c $<in> -o $<out> -L $<libmath.dir>
    }
