register
    cook.probe("cards", {
        inputs  = {},
        produce = [[ return { name = "not-an-array" } ]],
    })

recipe deal
    for_each cards
    cook "build/$<in.id>.txt" using {
        printf '%s\n' "$<in.id>" > $<out>
    }
