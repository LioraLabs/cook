Asserts CS-0126's parse-time rejection of the bare `NAME "value"` statement shape inside config blocks (the pre-CS-0011 VarDecl antipattern every make/just refugee types), reported with a did-you-mean diagnostic instead of a Lua VM traceback. The same shape check deliberately also rejects paren-less Lua call statements (e.g. `print "x"`) inside config blocks; write the parenthesized form.
