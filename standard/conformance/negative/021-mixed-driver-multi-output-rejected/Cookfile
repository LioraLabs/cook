recipe "libmath"
    gather "src/math/*.c"
    cook "build/lib/$<in.stem>.o" { cc -c $<in> -o $<out> }

recipe "build"
    gather "src/*.c"
    cook "$<in.stem>.o" "$<libmath.stem>.bin" {
        do-stuff
    }
