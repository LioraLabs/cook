register
    cook.probe("cards", {
        inputs  = {},
        produce = [[ return { name = "not-an-array" } ]],
    })

recipe deal
    ingredients cards
    cook "build/$<in.id>.txt" {
        printf '%s\n' "$<in.id>" > $<out>
    }
