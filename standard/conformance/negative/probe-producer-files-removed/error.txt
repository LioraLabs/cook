`files` and `tools` are top-level declarations; for determinants:
    files core:src
        "src/**/*.rs" "Cargo.toml"
    tools core:toolchain
        cc ld
    recipe build
        seal core:src core:toolchain
