recipe noop
    >>{
        cook.add_unit({
            name = "u",
            inputs = {},
            outputs = {"out"},
            probes = {"missing"},
            command = "true",
        })
    }
