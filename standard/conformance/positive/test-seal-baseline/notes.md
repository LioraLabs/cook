CS-0159: the recipe-level `seal` baseline applies to every *cacheable* unit,
which includes `test` units — not only `cook` units. A test keys on its sealed
probes' values exactly as a cook unit does (§17.4 rule 1).
