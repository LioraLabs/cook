use rockmod

recipe smoke
    test >{
        local m = require("rockmod")
        print(m.value)
    }

chore verify
    > if rockmod.value ~= 42 then error("rockmod.value mismatch") end
    > print("ok")
