COOK-171: the `nondet` trailing `share_mod` (the renamed v3 `record`) declares the
output intrinsically non-reproducible; it sets the disposition's `record` flag
(internal AST field unchanged). No determinant is added; the key is unchanged.
