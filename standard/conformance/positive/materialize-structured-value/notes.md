Pins the public shape of a structured `cook.materialize` call. Runtime cache
and worker behavior is covered by `cli/crates/cook-cli/tests/materialize.rs`;
the corpus only asserts that the Cookfile remains parseable and codegen-valid.
