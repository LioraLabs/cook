COOK-171: `seal` is a recipe-body step (a determinant input stream, sibling of
`ingredients`). It contributes its probe refs to the recipe-level baseline that
folds into every cook's effective seal set.
