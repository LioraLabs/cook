Cookfile
  uses: []
  imports: []
  config_blocks: [ConfigBlock name=None body="    var.optimize = \"-O0\"\n    var.symbols  = true\n    var.jobs     = 4" line=1, ConfigBlock name=Some("release") body="    var.optimize = \"-O2\"\n    var.symbols  = false" line=6]
  recipes:
    Recipe name="build" line=15
      deps: []
      ingredients: ["src/main.c"]
      excludes: []
      steps:
        Cook outputs=["build/main"] body=ShellBlock(["cc $<optimize> $<in> -o $<out>"])
  chores:
  register_blocks: [RegisterBlock body="    local flags = var.optimize\n    if var.symbols then flags = flags .. \" -g\" end\n    CFLAGS = flags .. \" -j\" .. var.jobs" line=10]
  top_level_module_calls: []
