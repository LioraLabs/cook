COOK-171: recipe-level `seal` steps stack additively into the baseline applied
to every cook; the trailing `nondet` modifier sets the disposition's `record`
flag (the renamed v3 `record`). Effective seal set is sorted + de-duplicated.
