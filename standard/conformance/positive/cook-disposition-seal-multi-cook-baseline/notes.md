# cook-disposition-seal-multi-cook-baseline

Pins §8.4.3 rule 1: a recipe-level `seal` is the baseline applied to **every**
`cook` unit in the recipe. One `seal host` step → both `a.o` and `b.o` carry
`seal=["host"]`. (COOK-172, CS-0117.)
