Pins §28.3.11 (v0.14+ / CS-0151) cc.compile_commands as a TOP-LEVEL
module_call — a column-0 single-line `cook_cc.compile_commands()`, not a
recipe-body call. The finalizer/write behavior is blessed-module behavior
(§28), not language surface. Parse-only.
