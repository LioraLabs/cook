Pins multi-line multi-output `cook` step (CS-0078). Each subsequent quoted output
pattern on its own line is valid; the `using` clause follows the last output.

Exercises App. A.4 (cook_step production, multi-line whitespace rule).
