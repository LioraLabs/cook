CS-0159 rule 1: a recipe with no `cook` unit but at least one `test` unit is a
legitimate seal target — the baseline has somewhere to land. This inverts the
pre-CS-0159 behaviour, which rejected the recipe with "no cook units to apply
to".
