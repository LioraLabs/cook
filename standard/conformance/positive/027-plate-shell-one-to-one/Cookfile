recipe install
    ingredients "src/*.c"
    cook "build/bin/$<in.stem>" using { cc $<in> -o $<out> }
    plate {
        install -d $PREFIX/bin
        install -m755 $<in> $PREFIX/bin/$<in.name>
    }
