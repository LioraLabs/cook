# README Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Write a hype landing-page style README.md for Cook that progressively reveals capabilities and reflects the author's voice.

**Architecture:** Single file (`README.md`) at project root. Content follows the structure defined in `docs/superpowers/specs/2026-03-12-readme-design.md`. All examples are pulled from or based on the existing `examples/Cookfile`.

**Tech Stack:** Markdown

**Spec:** `docs/superpowers/specs/2026-03-12-readme-design.md`

---

## Chunk 1: Write README.md

### Task 1: Write the README

**Files:**
- Create: `README.md`

- [ ] **Step 1: Write the full README.md**

Write `README.md` at the project root with the following sections in order. Voice: developer-casual with landing-page energy. Personal and authentic, not generic marketing copy.

**Section 1 — Header + Tagline:**
```markdown
# Cook

**A modern build system with the power of Make, the clarity of Just, and embedded Lua scripting.**

My vibe coded prototype of my dream build system.
```

**Section 2 — Progressive Examples:**

Three code blocks with short intro text between them, building from simple to powerful:

1. "As a task runner" — `setup` and `clean` recipes, shell commands only
2. "As a build system" — `build` recipe with `ingredients`, `serves`, `requires`, shell command
3. "With embedded Lua" — `lib` recipe with `>{` block, `recipe.ingredients`, `cook.sh()`, `fs` API

Use the exact Cookfile snippets from the spec (lines 28-67). Add a short punchy sentence before each tier to frame what it shows.

**Section 3 — "Why Cook?":**

Use the author's exact words from the spec (lines 76-78) as the opening. Then add a short line positioning Cook as the hybrid answer — something like "Cook is my answer — a hybrid task runner and build system that you can actually read."

**Section 4 — Feature Highlights:**

Use the bullet list from the spec (lines 86-91). Format as bold label + dash + description. Keep each bullet to one line.

**Section 5 — CLI:**

```
cook [recipe]         Run a recipe (defaults to "build")
cook serve [recipe]   Watch files, re-run on change
cook menu             List all recipes
cook init             Create a starter Cookfile
cook --plate          Print the generated Lua (debug)
```

Format as a code block or markdown table — whichever reads cleaner.

**Section 6 — The Dream (Roadmap):**

Short section with 3 bullets from spec (lines 107-109). Frame it aspirationally — this is where Cook is headed. Include the "imagine CMake as a Cook plugin" line for the plugin system bullet.

**Section 7 — Makefile vs Cook:**

Use the exact Makefile and Cookfile from the spec (lines 117-198). Add a short intro framing the comparison — something like "Still not convinced? Here's the same C library project..." and a short outro highlighting the key contrasts (tab-sensitivity, `$@` cryptography, `.PHONY` boilerplate, readability).

- [ ] **Step 2: Review the README for tone and accuracy**

Read through the complete README. Verify:
- All Cookfile examples use valid Cook syntax
- The Makefile example is realistic and correct
- The author's personal words are used verbatim
- Tone is consistent throughout (developer-casual, landing-page energy, not corporate)
- No features are claimed that don't exist in the codebase

- [ ] **Step 3: Commit**

```bash
git add README.md
git commit -m "docs: add README — introducing Cook"
```
