recipe app
    ingredients "src/greeting.txt"
    cook "out/app.txt" { tr 'a-z' 'A-Z' < $<in> > $<out> }

recipe check: app
    test { grep -q HELLO $<app> }
    test { test -s $<app> }
    test { ! grep -q lowercase $<app> }
