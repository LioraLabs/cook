probe target_list
    seal "data/targets.txt"
    lines { cat data/targets.txt }

recipe targets
    gather target_list
    cook "out/$<in>.cfg" { printf 'target=%s nonce=%s\n' '$<in>' "$(date +%s%N)" > $<out> }
