recipe io_popen_probe
    >{
        -- CS-0045: io.popen is a Lua escape hatch that bypasses
        -- cook.sh and the unit's command_hash. In a cook step it
        -- MUST raise.
        local h = io.popen("id")
        print(h:read("*a"))
    }
