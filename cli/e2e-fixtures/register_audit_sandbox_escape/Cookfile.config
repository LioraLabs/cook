config
    -- CS-0045: register-phase fs.* is sandboxed too. A config block
    -- runs at registration; calling fs.read on /etc/passwd here MUST
    -- raise before any unit is captured.
    var.LEAK = fs.read("/etc/passwd")

recipe register_phase_probe:
    @echo "should not get here"
