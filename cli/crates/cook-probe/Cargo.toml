[package]
name = "cook-probe"
version.workspace = true
edition = "2021"
description = "Shared stateful probe lifecycle for the Cook build system"

[dependencies]
cook-contracts = { path = "../cook-contracts" }
cook-cache = { path = "../cook-cache" }
serde_json = "1"

[dev-dependencies]
tempfile = "3"
