[package]
name = "cook-lang"
version.workspace = true
edition = "2021"
description = "Cookfile parser: text in, AST out"

[dependencies]
thiserror = "2"
cook-contracts = { path = "../cook-contracts" }
