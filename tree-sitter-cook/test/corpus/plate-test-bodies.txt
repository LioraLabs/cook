================
Removed plate step is rejected
================

recipe run
    plate { ./run }

---

(source_file
  (recipe
    (explicit_recipe_header
      name: (identifier)))
  (ERROR
    (import_name)
    (lua_code)))

================
Removed recipe loose shell is rejected
================

recipe run
    ./run

---

(source_file
  (recipe
    (explicit_recipe_header
      name: (identifier)))
  (ERROR
    (lua_code)))

================
Removed recipe Lua line is rejected
================

recipe run
    > print("run")

---

(source_file
  (recipe
    (explicit_recipe_header
      name: (identifier)))
  (ERROR
    (lua_code)))

================
Removed recipe Lua block is rejected
================

recipe run
    >{
      print("run")
    }

---

(source_file
  (recipe
    (explicit_recipe_header
      name: (identifier)))
  (ERROR
    (lua_code)))

================
Removed interactive command is rejected
================

recipe run
    @./run

---

(source_file
  (recipe
    (explicit_recipe_header
      name: (identifier)))
  (ERROR
    (lua_code)))

================
Removed inline Lua line is rejected
================

recipe run
    >> print("run")

---

(source_file
  (recipe
    (explicit_recipe_header
      name: (identifier)))
  (ERROR
    (lua_code)))

================
Removed inline Lua block is rejected
================

recipe run
    >>{
      print("run")
    }

---

(source_file
  (recipe
    (explicit_recipe_header
      name: (identifier)))
  (ERROR
    (lua_code)))
