use pnpm

recipe install
    pnpm.install()

recipe build: install
    pnpm.run("build")

recipe check: build
    ingredients ""
    pnpm.run("test")
    cook "" using "packages/monorepo-a/Cookfile"
    serve ""

chore clean
    rm -rf packages/*/dist packages/*/.cook .cook
