# Web app Cookfile — depends on core_lib via the workspace dep chain.
# web_app receives the core.o path via the linking step in the root Cookfile.

recipe web_obj
    ingredients "src/app.c"
    cook "build/web.o" using {
        mkdir -p build
        gcc -c src/app.c -o {out}
    }
