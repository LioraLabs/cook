use pnpm

recipe install
    >> pnpm.install()

recipe build: install
    >> pnpm.run("build")

recipe check: build
    ingredients ""
    >> pnpm.run("test")
    serve ""

chore clean
    rm -rf packages/*/dist packages/*/.cook .cook
